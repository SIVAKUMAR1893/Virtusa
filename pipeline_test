pipeline {
    agent any

 

    stages {

        

        stage("Publish NUnit Test Report") {
            steps {
                nunit testResultsPattern: 'test.xml'
            }
        }

        

    }
}
